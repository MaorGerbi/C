#include "hash_mapint.hpp"
/*
namespace experis {

ListItr HashMapInt::findKey(int a_key) {
	size_t index = m_hashFunc(a_key) % size();
	ListItr it = ((*this)[index]).begin();
	while(it != ((*this)[index]).end()) {
		if(a_key == *it.first) {
			return it;
		}
		++it;
	}
	return ((*this)[index]).end();
}
*/
}// experis

